{
  "version": 2,
  "buildCommand": "node -e \"console.log('No build step required')\"",
  "outputDirectory": "public",
  "builds": [
    {
      "src": "api/[...all].js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/[...all].js"
    }
  ]
}
